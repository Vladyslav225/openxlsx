Написать класс который имеет ряд методов: 
1 принимает лист -> __init__(self, list)
2 метод конвертирования листа в дикт [1,2,3] -> {1:1, 2:2, 3:3}
3 метод сохранения конвертированного дикта в файл 
4 метод считывания запписанной информации из файла 

5 конвертация дикта в ексель ( через библиотеку openpyxl )
 - https://openpyxl.readthedocs.io/en/stable/tutorial.html
6 считывание екселя в дикт ( через библиотеку openpyxl )

Все делаем через методы у класса 

